# Interface: ChordListParams

Arguments for `chord list`.

## Properties

### clipUuid

```ts
clipUuid: string;
```

UUID of the chord clip to read, with or without braces. `track list --type chord` reports the chord track; `clip list` on it yields the clips.
