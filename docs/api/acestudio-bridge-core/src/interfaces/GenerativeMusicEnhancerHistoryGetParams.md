# Interface: GenerativeMusicEnhancerHistoryGetParams

Arguments for `generative music-enhancer history get`.

## Properties

### taskId

```ts
taskId: string;
```

The generation's task id, as `history list` reports it — the first half of every result id.
