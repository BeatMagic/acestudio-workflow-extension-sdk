# Interface: MidiparamListLanesParams

Arguments for `midiparam list-lanes`.

## Properties

### clipUuid

```ts
clipUuid: string;
```

Clip id, as reported by `clip list` (braced form). Must be a GenericMidi clip.
