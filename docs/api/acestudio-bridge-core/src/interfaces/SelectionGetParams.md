# Interface: SelectionGetParams

Arguments for `selection get`.

## Properties

### scope?

```ts
optional scope?: string;
```

Selection scope: `arrangement` (timeline) or `editor` (piano-roll note selection). Omitted reads the arrangement.
