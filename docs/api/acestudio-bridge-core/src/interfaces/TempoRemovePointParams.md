# Interface: TempoRemovePointParams

Arguments for `tempo remove-point`.

## Properties

### pos

```ts
pos: number;
```

The point to remove. Ticks (`3840t`), clock time (`1.5s`), or musical position (`4.1.0`). No point there is a NOT_FOUND. See `help time-values`.
