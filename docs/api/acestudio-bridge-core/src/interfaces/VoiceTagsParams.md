# Interface: VoiceTagsParams

Arguments for `voice tags`.

## Properties

### type

```ts
type: string;
```

Sound source type to return filters for. `voice` returns supported languages; `instrument` returns categories.
