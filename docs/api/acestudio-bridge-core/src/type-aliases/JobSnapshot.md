# Type Alias: JobSnapshot

```ts
type JobSnapshot = JobGetResult;
```

A job as the ledger reports it — lifecycle, progress where the class declares
it, and the result children, each settling on its own (ADR 0084).
