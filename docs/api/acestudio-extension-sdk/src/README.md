# acestudio-extension-sdk/src

The SDK for building ACE Studio workflow extensions — the extension layer
above [the bridge core](https://www.npmjs.com/package/@timedomain/acestudio-bridge-core).

## Remarks

This is the package skeleton; the public API is introduced in subsequent
releases. The browser-only page-side channel ships from the `./page` subpath.

## Variables

- [packageName](variables/packageName.md)
