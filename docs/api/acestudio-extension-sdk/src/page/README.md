# acestudio-extension-sdk/src/page

The browser-only page side of an extension's UI channel, imported from
`@timedomain/acestudio-extension-sdk/page`.

## Remarks

This is the package skeleton; the public API is introduced in subsequent
releases. This entry is a browser build and shares no runtime code with the
process-side entry.

## Variables

- [packageName](variables/packageName.md)
