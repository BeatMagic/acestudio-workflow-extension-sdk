# Variable: packageName

```ts
const packageName: "@timedomain/acestudio-extension-sdk/page" = "@timedomain/acestudio-extension-sdk/page";
```

Placeholder export that anchors the page subpath's public surface while the
skeleton has no API yet.
