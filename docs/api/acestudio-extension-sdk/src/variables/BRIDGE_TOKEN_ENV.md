# Variable: BRIDGE\_TOKEN\_ENV

```ts
const BRIDGE_TOKEN_ENV: "ACE_EXTENSION_BRIDGE_TOKEN" = "ACE_EXTENSION_BRIDGE_TOKEN";
```

The one-time session token this process authenticates its handshake with.
