# Type Alias: ProjectRelocation

```ts
type ProjectRelocation = ProjectRelocatedParams;
```

What [ExtensionDefinition.resume](../interfaces/ExtensionDefinition.md#resume) is handed: where the project folder is
now. Unchanged from what the extension already had means the move was abandoned.
